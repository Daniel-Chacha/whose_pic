services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    image: whosepic-backend
    container_name: whosepic-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
